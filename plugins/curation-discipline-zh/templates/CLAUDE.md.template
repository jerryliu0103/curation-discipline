# <專案名稱>

<!--
  策展資料集的專案層規則骨架。

  這裡放什麼的原則：CLAUDE.md 每一次對話都會被載入，所以它只該放「漏讀就會造成損害」
  的東西。通用方法放在 skill 裡；這個檔案放**這個專案專屬的事實**，以及**這個專案裡
  已經發生過的事故**。

  用不到的段落請整段刪掉。一份準確的短文件，勝過一份沒有人讀完的完整文件。
-->

## 目的

<一段話：這份資料集是做什麼用的、誰在讀>

## 正本

- **正本**：<真正的資料放在哪裡>
- **衍生的，永遠不是權威**：<鏡像、匯出、本機副本、快取>
- **怎麼連到它**：<確切的指令>
- **陷阱**：<哪些指令或變數會連到錯的副本，而且不會報錯>

> **陷阱要明確寫出來。** 一個連到錯目標卻回傳合理資料、離開碼還是 0 的指令，
> 會被用好幾個月才有人發現。

## 寫入流程——所有代理都遵守

1. `curator` 蒐集到 `data/staging/*.json`——不寫正本
2. `validator` 檢查格式、來源、重複、邏輯——什麼都不寫
3. 人讀那份報告
4. 人執行寫入

方法與理由在 `curation-pipeline` skill。這裡寫的是這個專案專屬的部分：

- <代理可以提議寫入哪些表>
- <哪些欄位是必填>
- <哪些操作需要第二個人看過>

## 來源欄位要求

每一列都帶：

- `source`——誰說的
- `retrieved_at`——哪一天蒐集的
- <這個專案專屬的座標：量測條件、權利狀態、版本>

## 去重用的來源座標

| 表 | 座標 |
|---|---|
| <表名> | <能識別來源位置的欄位組合> |

**比對文字抓不到重複匯入**——重新匯入時每次的措辭都不一樣。
見 `provenance-and-dedup` skill。

## 命名與項目解析

- 解析順序：<本名 → 別名 → 次要軸>
- **解析不到的名字絕不自動建檔。** 寫進 `data/staging/unresolved.json` 交給人決定。
- 受控詞彙：<放在哪裡>。**沒有合用的值時留 NULL 並回報**——不要拿最接近的詞代替。

## 這個專案的事故紀錄

<!--
  最有價值、而且一開始一定是空的那一段。

  每一件「出了錯而且沒有報錯」的事寫一條。標日期、寫代價、寫現在靠什麼抓。
  以後有人問「這條規則為什麼在這裡」，答案就在這裡——**而如果這一段是空的，
  這條規則遲早會被一個看不出它是為了什麼的人刪掉。**

  格式：
  ### <日期>——<一句話摘要>
  發生了什麼。是什麼讓它安靜。現在靠什麼抓。
-->

## 排程工作

- <工作>：<何時跑>——<產出什麼、寫到哪裡>
- **排程的產出永遠不是權威。** 它落在 staging，跟其他東西一樣要經過人核准。

## 開發規則

- <改完之後必須跑的測試／檢查／review 指令>
- <哪些東西必須自己 commit，不可以留給自動化掃走>
