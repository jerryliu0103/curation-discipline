{
  "description": "Ask before a Bash command reaches a superseded or non-canonical datastore. Edit PATTERNS in canonical-store-guard.py to match your project's own traps — as shipped it matches example patterns and will fire on nothing in your project.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/canonical-store-guard.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
